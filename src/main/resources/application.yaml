spring:
  application:
    name: usermanagement

  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port : 8082


  jwt:
    secret: fnb-b4-bootcamp-shared-jwt-signing-secret-change-me-32bytes-min
    expiration: 3600000